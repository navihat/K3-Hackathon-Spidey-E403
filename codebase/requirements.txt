discord.py==2.7.1     # lớp ① — đọc/gửi tin nhắn Discord
                      # KHÔNG hạ xuống 2.4: bản đó import audioop, bị xoá khỏi
                      # stdlib từ Python 3.13 (PEP 594) -> ModuleNotFoundError
google-genai>=1.0     # lớp ③ — gọi Gemini (free tier Google AI Studio)
pydantic>=2.9         # schema quyết định AI
python-dotenv>=1.0    # đọc .env khi chạy local
PyYAML>=6.0           # đọc eval/golden-set.yaml
pytest>=8.3           # test
